type NoteRecord {
  noteId: String!
}

type Note {
  noteId: String!
  body: String!
}

type Query {
  allNotes: [NoteRecord!]!
}

type Mutation {
  createNote(noteId: String!, createdAt: String!): NoteRecord!
}
