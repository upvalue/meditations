{
  "name": "techne",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:upvalue/meditations.git",
  "author": "Phil <phil@upvalue.io>",
  "license": "MIT",
  "scripts": {
    "build": "cd frontend && yarn && yarn build",
    "start:frontend": "cd frontend && yarn start",
    "start:backend-compile": "cd backend && yarn watch",
    "start:backend": "cd backend && yarn start",
    "start:docker": "docker-compose -f docker-compose.yml up",
    "start:graphql": "cd shared && yarn generate",
    "start": "npm-run-all -l -p start:*"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
