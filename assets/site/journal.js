// Generated by CoffeeScript 1.10.0
(function() {
  var create, entry_new, initialize, main, view;

  initialize = function() {
    console.log('Journal: initializing');
    if (typeof html5 !== "undefined" && html5 !== null) {
      html5.addElements('scope task scope-days');
    }
    initialize = function() {
      return false;
    };
    return true;
  };

  entry_new = function() {
    return console.log("Entry-new");
  };

  view = function(date) {
    return $.get("/journal/entries?date=" + date, function(entries) {
      return riot.mount('entries');
    });
  };

  create = function(date) {
    return true;
  };

  main = function() {
    initialize();
    $("#journal-new-entry-date").datepicker({
      onSelect: function(datestr) {
        var date;
        date = moment(datestr, "MM/DD/YYYY");
        return riot.route("create/" + (date.format('YYYY-MM-DD')));
      }
    });
    riot.route(function(action, date) {
      switch (action) {
        case 'view':
          return view(date);
        default:
          return true;
      }
    });
    riot.route.start(true);
    return riot.route("view/" + (moment().format('YYYY-MM')));
  };

  window.Journal = {
    initialize: initialize,
    main: main
  };

}).call(this);

//# sourceMappingURL=journal.js.map
